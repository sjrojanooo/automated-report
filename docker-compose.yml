version: "3.9"
services:
    test:
      image: "automated-report"
      volumes:
        - .:/app
      command: python3 -m pytest
    run:
      image: "automated-report"
      volumes:
        - .:/app
      command: /python3 main.py